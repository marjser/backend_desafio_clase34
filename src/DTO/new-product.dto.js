
class NewProductDto {
	constructor(newProdData){
		this.name = newUserInfo.name
		this.lastname = newUserInfo.lastname
		this.fullname = `$newUserInfo.name} ${newUserInfo.lastname}`
		this.email = newUserInfo.email
		this.role = 'role'
    }
}

module.exports = NewProductDto
